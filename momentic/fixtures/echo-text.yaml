type: momentic/fixture
schemaVersion: 1.0.0
name: echo-text
description: A fixture that echoes text
setup:
  steps:
    - run: "sleep 5 && echo 'setup is done'"
  timeout: 10
teardown:
  steps:
    - run: |
        echo 'teardown is happening'
        echo 'this is a multiline yaml'
  timeout: 30

